# RNN/SNN N-MNIST
  Here we add the normalize function in backward function of SNNs.
  As we set the lens as 0.5 in PTB and S-MNIST, therefore, there is no need for add (2 * lens) item in their backward functions.
